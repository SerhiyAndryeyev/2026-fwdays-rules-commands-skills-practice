# Export Presentation to PDF

This workflow exports the Slidev presentation into a static PDF format.

## Step 1: Install playwright (if needed) and export
// turbo
```bash
pnpm install playwright-chromium
pnpm slidev export slides.md --format pdf
```
